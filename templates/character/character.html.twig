{% extends 'base.html.twig' %}

{% block title %}{{ character.name }}{% endblock %}

{% block body %}
    <div class="character-profile">
        
        <h1>{{ character.name }}</h1>
        <ul>
            <li>{{ character.element }}</li>
            <li>{{ character.rarity }}</li>
        </ul>
    </div>

{% endblock %}
